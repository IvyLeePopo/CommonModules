TCP 网络相关的流程：
1、接收到数据，校验基本信息：数据完整性，帧头帧尾，主要分包，沾包；
（1）拿到数据，看是不是不止一条信息
（2）找帧头；
（3）找到头部，再找帧尾
（4）拿到完整的一帧数据
（5）去掉帧头、帧尾，去转义
（6）获取数据的长度，数据内容等信息


2、发送数据：先组包，校验，再转义，添加帧头和帧尾，最后发送出去；
（1）先组装里面的数据;
（2）数据长度；
（3）帧编号；
（4）该条之类的指令编号+子命令编号；
（5）真正的数据；
（6）添加校验数据crc16()；
（7）对以上的数据进行转义；
（8）再添加上帧头和帧尾；